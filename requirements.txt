# GATS — runtime requirements
numpy>=1.24.0
matplotlib>=3.7.0

# Optional: only needed if you run the LLM-backed baselines
# (python reproduce.py uses the default --backend mock and does NOT need this)
# requests>=2.28.0

# Optional: dev/test
# pytest>=7.0
